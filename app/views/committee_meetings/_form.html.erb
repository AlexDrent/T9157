<%= form_with(model: committee_meeting) do |form| %>
  <% if committee_meeting.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(committee_meeting.errors.count, "error") %> prohibited this committee_meeting from being saved:</h2>

      <ul>
        <% committee_meeting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
