<p style="color: green"><%= notice %></p>

<%= render @agenda_item %>

<div>
  <%= link_to "Edit this agenda item", edit_agenda_item_path(@agenda_item) %> |
  <%= link_to "Back to agenda items", agenda_items_path %>

  <%= button_to "Destroy this agenda item", @agenda_item, method: :delete %>
</div>
