<%= form_with(model: [@plc_meeting, @agenda_item]) do |form| %>
  <% if agenda_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(agenda_item.errors.count, "error") %> prohibited this agenda_item from being saved:</h2>

      <ul>
        <% agenda_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :subject, style: "display: block" %>
    <%= form.text_area :subject %>
  </div>
  <%= form.hidden_field :plc_meeting_id %>
  <div>
    <%= form.label :details, style: "display: block" %>
    <%= form.text_area :details %>
  </div>

  <div>
    <%= form.label :done, style: "display: block" %>
    <%= form.check_box :done %>
  </div>
  <div>
    <%= form.label :resolutions, style: "display: block" %>
    <%= form.text_area :resolutions %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
